# Punto Native - переключатель раскладки для macOS

Русский · [English](README.en.md)

Punto Native - альтернатива Punto Switcher для Mac с русской и английской раскладками. Написали `ghbdtn` вместо «привет» - нажмите **⌘⌥⇧**, и слово исправится. Можно включить автоматическое переключение или исправлять текст вручную.

Приложение написано на Swift, AppKit и SwiftUI. Живёт в строке меню. Работает в браузерах, редакторах и терминалах; во время разработки проверялось в Safari, Codex, Terminal и Ghostty.

**[Скачать для Mac](https://github.com/rshagiev/punto-switcher/releases/latest)** · macOS 14+ · Apple Silicon (M1 и новее)

## Установка

1. Скачайте DMG, откройте его и перетащите **PuntoNative.app** в «Программы».
2. Запустите приложение и разрешите «Универсальный доступ» и «Мониторинг ввода» в системных настройках.
3. Если пользуетесь другим переключателем раскладки, завершите его.

Всё необходимое для переключения уже внутри приложения. Дополнительно устанавливать Punto Switcher не нужно.

Сборка пока без нотарификации Apple. Если macOS блокирует запуск, откройте «Системные настройки → Конфиденциальность и безопасность» и разрешите открытие приложения.

## Раскладка под рукой

**⌘⌥⇧** исправляет последнее набранное слово или выделенный текст. Сочетание можно поменять в настройках, как и клавиши для смены регистра и отмены конвертации. Поддерживаются и обычные сочетания, и сочетания только из модификаторов.

Для каждого приложения можно выбрать свой режим. Например, в мессенджере включить автопереключение, а в редакторе кода оставить только ручное. Перетащите приложение из Finder в раздел «Профили» и выберите нужный вариант.

В настройках также есть:

- свои правила для слов, фрагментов и регулярных выражений;
- обучение на отменённых исправлениях;
- запоминание раскладки для каждого приложения;
- короткая плашка с раскладкой после исправления;
- звуки и запуск при входе в macOS.

Значок появляется в Dock, когда открыты настройки или окно проверки текста. Закрыли окно - приложение продолжает работать в строке меню.

## Привести текст в порядок с Luna

Набрали текст с опечатками или надиктовали длинную мысль? Выделите нужный фрагмент и вызовите Luna. Без выделения она возьмёт текущий абзац.

Есть две команды:

- **Исправить сразу** - исправить опечатки и грамматику прямо в поле ввода.
- **Проверить перед заменой** - открыть «Было / Стало» и посмотреть изменения.

Обеим можно назначить свои сочетания в разделе Luna. Если во время запроса продолжить печатать или перейти в другое поле, быстрая замена отменится.

В окне проверки доступны три режима:

| Режим | Для чего |
| --- | --- |
| **Ошибки** | Поправить опечатки, пунктуацию и грамматику, сохранив манеру речи. |
| **Собрать мысль** | Разобрать надиктованный текст: убрать случайные повторы, учесть самопоправки, расставить абзацы. |
| **Промпт** | Превратить сумбурное описание задачи в понятный запрос для LLM: что сделать, с каким контекстом и ограничениями. |

Не нравится отдельная правка? **Нажмите на подсветку** - в результате останется исходный вариант. Ещё один клик вернёт исправление. Можно выбрать нужные правки, а затем скопировать или применить текст.

Режим можно менять даже во время генерации: текущий запрос отменится. Готовые варианты сохраняются в памяти для этого текста, поэтому возвращение к ним не расходует новый запрос. Для сумбурной диктовки приложение может само предложить «Собрать мысль»; выбор всегда можно изменить.

Luna иногда ошибается или теряет детали. Важные числа, имена и формулировки лучше проверить перед применением. Последнее сравнение и отмена замены доступны из меню приложения.

### Как подключить

Установите [Codex CLI](https://developers.openai.com/codex/cli/) и войдите в свой аккаунт. В настройках Luna появится **«Codex подключён»**. Кнопка «Попробовать на примере» проверит работу на готовом тексте.

Используется `gpt-5.6-luna`. Отдельный API-ключ не нужен, запросы расходуют лимиты вашего аккаунта Codex. Без Codex все функции переключения раскладки продолжают работать.

## Что происходит с текстом

Переключение раскладки работает локально. Luna отправляет в Codex только выбранный текст или текущий абзац и только по вашей команде. Постоянного наблюдения через модель нет.

Punto не ведёт историю набора и не собирает телеметрию. На Mac остаются настройки, дневные счётчики и слова для обучения, если оно включено. Настройки хранятся в `~/Library/Application Support/PuntoNative`. Обработка запросов Luna также зависит от настроек и правил вашего аккаунта Codex.

## Частые вопросы

### Чем заменить Punto Switcher на Mac с Apple Silicon?

Punto Native исправляет текст, набранный в неверной раскладке, вручную или автоматически. Версия 2.0 работает на macOS 14 и новее, на Mac с M1 и последующими чипами Apple. Intel не поддерживается.

### Как исправить уже набранный текст в другой раскладке?

Нажмите **Command + Option + Shift**, чтобы исправить последнее слово. Для фразы сначала выделите её. Например, `ghbdtn` превратится в «привет», а `руддщ` - в `hello`. Сочетание можно изменить в настройках.

### Можно ли пользоваться без ChatGPT и интернета?

Да. Конвертация раскладки, правила и профили приложений работают локально. Codex нужен только для дополнительного редактирования текста с Luna, которое запускается по вашей команде.

### Работает ли исправление раскладки в терминале и Codex?

Во время разработки проверялись Terminal, Ghostty, Codex и Safari. Защищённые поля пропускаются. Если в конкретном редакторе замена не срабатывает, [создайте issue](https://github.com/rshagiev/punto-switcher/issues), указав приложение, версию macOS и шаги воспроизведения.

## Собрать из исходников

Нужны Swift 6 и Xcode Command Line Tools.

```sh
swift run -c release PuntoChecks
bash Scripts/build-app.sh
open 'Build/Punto Native.app'
```

Скрипт использует ad-hoc подпись. Для локальных обновлений с постоянным сертификатом:

```sh
CODE_SIGN_IDENTITY='Your signing identity' bash Scripts/deploy.sh
```

Собрать DMG: `bash Scripts/package-release.sh`.

[Архитектура](docs/ARCHITECTURE.md) · [Проверки](docs/VERIFICATION.md) · [История разработки](docs/HISTORY.md) · [Что нового](CHANGELOG.md)
